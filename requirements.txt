# =====================================================================
# SDP-2026 pip requirements
# Covers the unified API (sdp/), the AllClear benchmark engine, Proba-V
# data loaders, visualization and metrics tooling.
#
# Install:
#     pip install -r requirements.txt
#
# If you are using the conda environment in environment.yml, pip will be
# invoked automatically on this file.
# =====================================================================

# --- Core deep learning (skip if installed via conda) ----------------
# torch>=2.1,<2.3
# torchvision>=0.16,<0.18
# torchaudio>=2.1,<2.3

# --- Scientific stack ------------------------------------------------
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
scikit-image>=0.21
tqdm>=4.65
Pillow>=10.0
PyYAML>=6.0
einops>=0.7
opencv-python>=4.8

# --- Geospatial / raster I/O ----------------------------------------
rasterio>=1.3
pyproj>=3.6
shapely>=2.0
fiona>=1.9
geopandas>=0.14
rioxarray>=0.15
xarray>=2023.8

# --- Image quality metrics ------------------------------------------
pyiqa>=0.1.10
torchmetrics>=1.2
lpips>=0.1.4

# --- Generative baselines / diffusion ------------------------------
diffusers>=0.25
transformers>=4.38
accelerate>=0.25
timm>=0.9

# --- AllClear benchmark engine deps --------------------------------
h5py>=3.9
netCDF4>=1.6
zarr>=2.16
dask>=2023.10
joblib>=1.3

# --- Download / cloud access ---------------------------------------
requests>=2.31
huggingface-hub>=0.20
gdown>=4.7

# --- CLI / packaging -----------------------------------------------
click>=8.1
rich>=13.7
colorama>=0.4.6

# --- Dev / test ----------------------------------------------------
pytest>=7.4
pytest-cov>=4.1

fvcore>=0.1.5

torch-fidelity>=0.4.0